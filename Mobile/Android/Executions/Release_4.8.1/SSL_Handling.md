| Test case | URL | Checkpoints | Expected result | Comments |
|:--:|:--|:--|:--|:--|
| Valid https flow | `https://badssl.com/` | 1. `KnownServersHostnameVerifier.verify(...)`<br>2. `mDelegate.verify(hostname, session)`<br>3. Final result of `verify(...)` | 1. Reached :white_check_mark:<br>2. `mDelegate.verify(...) == true` :white_check_mark:<br>3. `KnownServersHostnameVerifier.verify(...) == true` :white_check_mark:<br>3. No fallback to `isCertInKnownServersStore(...)` :white_check_mark:<br>3. No SSL dialog :white_check_mark: | The badssl.com is intentionally valid and is used as the valid https case. |
| Hostname not verified | `https://wrong.host.badssl.com/` | 1. `mDelegate.verify(hostname, session)`<br>2. `NetworkUtils.isCertInKnownServersStore(...)`<br>3. `getCertificateAlias(cert)`<br>4. `e instanceof SSLPeerUnverifiedException`<br>5. `mException` / `mCode`<br>6. `SslUntrustedCertDialog.m509Certificate`<br>7. Final button action | 1. `mDelegate.verify(...) == false`:white_check_mark:<br>2. `isCertInKnownServersStore(...) == false`:white_check_mark:<br>3. `getCertificateAlias(cert) == null`:white_check_mark:<br>4. `e instanceof SSLPeerUnverifiedException == true`:white_check_mark:<br>5. `mException instanceof CertificateCombinedException`:white_check_mark:<br>5. `mCode == SSL_RECOVERABLE_PEER_UNVERIFIED`:white_check_mark:<br>6. `m509Certificate == null`:white_check_mark:<br>6. Dialog shows only `OK`<br>7. Pressing `OK` calls `onCancelCertificate()` :white_check_mark:| This is the main scenario. The certificate does not cover `wrong.host.badssl.com`, so the app must not allow the user to save/trust it from the dialog and it is aborted. |
| Untrusted certificate, hostname correct | `https://self-signed.badssl.com/` | 1. `mDelegate.verify(hostname, session)`<br>2. `e instanceof SSLPeerUnverifiedException` <br>3. `SslUntrustedCertDialog.m509Certificate` <br>4. Final button action if user accepts | 1. `mDelegate.verify(...) == true` :white_check_mark:<br>1. No fallback to `isCertInKnownServersStore(...)` from hostname verifier :white_check_mark:<br>2. `e instanceof SSLPeerUnverifiedException == false` :white_check_mark:<br>3. `m509Certificate != null` :white_check_mark:<br>4. If user accepts: `addCertToKnownServersStore(...)` and `onSavedCertificate()` is called :white_check_mark:| The hostname is correct, but the certificate is self-signed. This should be treated as a classic trust problem, not as a peer/hostname verification failure. |
| Certificate validity error, hostname correct | `https://expired.badssl.com/` | 1. `mDelegate.verify(hostname, session)`<br>2. `e instanceof SSLPeerUnverifiedException` <br>3. `SslUntrustedCertDialog.m509Certificate`<br>4. UI Dialog | 1. `mDelegate.verify(...) == true` :white_check_mark:<br>1. No fallback to `isCertInKnownServersStore(...)` from hostname verifier :white_check_mark:<br>2. `e instanceof SSLPeerUnverifiedException == false` :white_check_mark:<br>3. `m509Certificate != null` :white_check_mark:<br>4. Dialog to continue with connection :white_check_mark: | The hostname matches, but the certificate is expired. This validates that certificate validity errors are not confused with peer/hostname failures. |
| Untrusted root CA, hostname correct | `https://untrusted-root.badssl.com/` | 1. `mDelegate.verify(hostname, session)`<br>2. `e instanceof SSLPeerUnverifiedException` <br>3. `SslUntrustedCertDialog.m509Certificate` <br>4. UI dialog | 1. `mDelegate.verify(...) == true` :white_check_mark:<br>1. No fallback to `isCertInKnownServersStore(...)` from hostname verifier :white_check_mark:<br>2. `e instanceof SSLPeerUnverifiedException == false`:white_check_mark:<br>3. `m509Certificate != null` :white_check_mark:<br>4. Dialog to continue with connection :white_check_mark: | The hostname matches, but the root CA is not trusted. This validates that trust-chain errors are not confused with peer/hostname failures. |
| Missing intermediate certificate, hostname correct| `https://incomplete-chain.badssl.com/` | 1. `mDelegate.verify(hostname, session)`<br>2. `e instanceof SSLPeerUnverifiedException` <br>3. `SslUntrustedCertDialog.m509Certificate` <br>4. UI dialog | 1. `mDelegate.verify(...) == true` :white_check_mark:<br>1. No fallback to `isCertInKnownServersStore(...)` from hostname verifier :white_check_mark:<br>2. `e instanceof SSLPeerUnverifiedException == false` :white_check_mark:<br>3. If dialog appears: `m509Certificate != null` :white_check_mark:<br>4. Dialog to continue with connection :white_check_mark: | The hostname matches, but the certificate chain is incomplete. This validates that chain-building errors are not confused with peer/hostname failures. |